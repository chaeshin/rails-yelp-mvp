<div class="mt-3 container">
  <h1>Add Reviews</h1>
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content%>
    <%= f.input :rating, collection: (1..5).to_a, include_blank: false %>
    <%= f.submit 'Submit', class: 'btn btn-light mt-2' %>
  <% end %>
</div>
